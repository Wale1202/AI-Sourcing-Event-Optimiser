# Copy to .env to override defaults.
# SQLite (default) — file in the backend/ working directory:
DATABASE_URL=sqlite:///./sourcing.db

# Postgres (later milestone):
# DATABASE_URL=postgresql+psycopg://user:password@localhost:5432/sourcing
